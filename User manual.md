# MV Trend Studio — 사용자 매뉴얼

버전: **v1.0.0-alpha.3**

## 1. 홈

홈은 “다음 뮤직비디오를 무엇으로 만들 것인가?”에서 시작합니다.

- **지금 빠르게 움직이는 기법**에서 상승 신호를 확인합니다.
- `조합에 추가`를 누르면 해당 기법이 Recipe Builder의 초기 선택값으로 들어갑니다.
- 익숙함이 강한 신호는 Watch / Avoid 영역에서 확인합니다.

## 2. 레시피 빌더

`콘셉트 → 컬러 → 카메라 → 모션 → 편집` 다섯 축을 조합합니다.

선택 순서는 고정되어 있지 않습니다. 한 축에 하나의 핵심 기법을 선택합니다.

오른쪽의 Production Fit 평가는 다음 네 항목으로 구성됩니다.

- 트렌드 모멘텀: 35점
- 근거 강도: 25점
- 포화 리스크: 20점
- 조합 응집도: 20점

최소 3개 축을 선택하면 결과 화면으로 이동할 수 있습니다.

## 3. 결과 등급

### 안전한 조합
검증 근거가 충분하고 제작 위험이 낮은 조합입니다.

### 지금 뜨는 조합
현재 트렌드 상승 신호와 제작 근거가 균형을 이룹니다.

### 차별화 조합
표본이 적거나 새로운 조합이지만 차별화 가능성이 높습니다.

등급은 우열이 아니라 제작 전략을 설명하기 위한 분류입니다.

## 4. 트렌드

2025 연간 대비 2026 YTD의 MV 등장 비율 변화를 비교합니다. 리포트 원문에 존재하는 이전 비율·현재 비율·증감폭을 그대로 노출합니다.

## 5. 감독 DNA

2편 이상의 MV가 분석된 감독을 대상으로 반복적인 콘셉트, 카메라, 컬러 문법을 보여줍니다. 표본이 적은 감독은 확정적인 시그니처로 단정하지 않습니다.

## 6. 분석 방법

원본 리포트의 분석 기간, 표본, 검증/추정 구분과 서비스 내부 점수의 차이를 확인할 수 있습니다.


## 7. 장면 근거 Drawer

Recipe Result에서 `장면 근거 보기` 또는 각 제작 축의 `타임코드` 버튼을 누르면 근거 패널이 열립니다.

- 데스크톱: 화면 오른쪽 Drawer
- 모바일: 화면 아래 Bottom Sheet
- 표시 정보: 기법, 아티스트, MV 제목, 구간, 타임코드, 원본 리포트 페이지
- 현재 표시는 원본 PDF Recipe 01–03에 직접 기재된 대표 장면에 한정됩니다.

프레임 이미지는 현재 버전에서 의도적으로 포함하지 않습니다. 공개 서비스에 영상 프레임을 넣기 전 사용 권한과 출처 정책을 먼저 확정합니다.


## 8. 레시피 저장과 공유

Recipe Result에서 다음 기능을 사용할 수 있습니다.

- **레시피 저장**: 현재 조합과 점수를 이 브라우저의 LocalStorage에 저장합니다. 최대 12개까지 유지합니다.
- **저장 목록**: `/recipes/saved`에서 저장한 레시피를 다시 열거나 삭제합니다.
- **공유 링크 복사**: 현재 5축 선택값을 URL query에 담아 복사합니다. 상대방은 같은 URL을 열면 동일한 조합을 볼 수 있습니다.
- **레시피 수정**: 결과의 선택값을 그대로 Recipe Builder로 넘겨 수정합니다.

현재 저장 기능은 계정 동기화가 아닙니다. 브라우저별 로컬 저장이며, 공유 URL은 별도 DB 없이 작동합니다.

## 9. 상단 내비게이션

현재 보고 있는 메뉴만 골드로 표시됩니다. `Recipe Builder`, `Recipe Result`, `Saved Recipes`는 모두 레시피 작업 흐름이므로 상단에서는 **레시피 빌더**가 활성 상태로 표시됩니다.
